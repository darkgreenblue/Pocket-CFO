# ROADMAP — کارهای آینده‌ی CFO جیبی

این فایل حافظه‌ی بلندمدتِ تصمیم‌ها و کارهای معوق است تا فراموش نشوند. هر «بعداً انجامش
می‌دهیم» اینجا ثبت می‌شود.

## در حال انجام / تصمیم‌های اخیر
- **⚠️ روی سرور یک `telegram-bot-api` محلی هست که پورت‌های ۸۰۸۱ و ۸۰۸۲ را گرفته.** درِ
  دومِ ورودی اولش روی ۸۰۸۱ گذاشته شد و دیپلوی با `port is already allocated` ترکید —
  و چون انتشارِ پورت در خودِ `docker-compose.yml` بود، کانتینر recreate شد و بالا نیامد،
  یعنی یک قابلیتِ اختیاری کلِ ربات را آفلاین کرد. حالا پورت **۸۷۹۱** است و انتشارش در
  `docker-compose.ingest.yml` جداست: دیپلوی اول با آن تلاش می‌کند و اگر نشد، ربات را
  بدونِ آن بالا می‌آورد. هر پورتِ تازه‌ای هم که خواستیم، اول باید با این دو چک شود.
- **درِ دومِ ورودی (میان‌برِ iOS):** ربات حالا علاوه بر تلگرام یک endpoint کوچکِ HTTP هم
  دارد. دلیلِ اصلی‌اش دسترسی است: گوشیِ کاربر همیشه به تلگرام نمی‌رسد ولی به سرورِ ما
  می‌رسد، پس جهتِ ترافیک برعکس شد — گوشی در دیتابیس می‌نویسد و **سرور** کارت را به
  تلگرام می‌فرستد. نکته‌ی معماری: نیازی به polling روی دیتابیس نشد چون سرورِ HTTP روی
  همان event loopِ ربات است و مستقیم `bot.send_message` می‌زند؛ به‌جای آن اگر ارسال
  شکست بخورد، `card_message_id` خالی می‌ماند و یک jobِ هر ۵ دقیقه دوباره تلاش می‌کند.
  مسیر عمداً باریک است (فقط ثبتِ تراکنش) و `tests/test_ingest.py` نگهبانِ این تصمیم است.
- **راه‌اندازیِ میان‌بر باید اسکیل‌پذیر بماند:** توکن‌ها از `.env` به دیتابیس منتقل شدند و
  با `/shortcut` صادر می‌شوند، چون توکنِ محیطی برای هر کاربرِ تازه یک ویرایشِ `.env` +
  ری‌استارت می‌خواست. به همین دلیل اندپوینتِ `/s/<token>` هم اضافه شد: توکن داخلِ URL و
  بدنه خودِ متن/فایلِ صوتی، تا میان‌بر سه اکشن بیشتر نباشد و کاربر هیچ JSONای دستی
  نسازد. هر فیلدِ اضافه در فرمِ شرتکات یعنی یک قدمِ بیشتر ضربدرِ تعدادِ کاربرها.
  چون میان‌برِ ساده `request_id` ندارد، کلیدِ تکراری‌نشدن از هشِ محتوا + دقیقه ساخته می‌شود.
- **فایلِ میان‌بر را نمی‌شود برای کاربر ساخت:** از iOS 15 فایل‌های `.shortcut` امضا
  می‌شوند و امضا فقط با `shortcuts sign` روی macOS ممکن است. مسیرِ محصولی، لینکِ iCloud
  به‌علاوه‌ی Import Questions است (فیلدی که Import Question دارد موقعِ اشتراک‌گذاری پاک
  می‌شود، پس لینکِ شخصی لو نمی‌رود). در `docs/ios-shortcut.md` توضیح داده شده.
- **دسترسی موقت:** محدودیتِ کاربران موقت است و روزی ربات برای همه باز می‌شود. برای همین
  کلیدش در کد است نه سرور: `ACCESS_MODE` در `bot/config.py`. بازکردنِ ربات برای همه =
  تغییرِ همان یک خط به `ACCESS_OPEN` + push (بدون SSH، بدون دست‌زدن به `.env`، بدون
  پاک‌کردنِ لیست تا اگر خواستیم برگردیم). آی‌دی‌های کد با `.env` **جمع** می‌شوند و اگر
  `.env` خالی باشد کد محدودش نمی‌کند. وضعیتِ مؤثر در لاگِ راه‌اندازی چاپ می‌شود، چون
  محتوای `.env` سرور از بیرون دیده نمی‌شود.
- **✅ دکمه‌ی «♻️ ریست تست» حذف شد** (پایانِ فازِ تست، شروعِ استفاده‌ی واقعیِ خانوار). هم
  دکمه، هم شاخه‌ی هندلرش، هم خودِ `repo.reset_user` برداشته شدند تا هیچ مسیری برای پاک‌کردنِ
  دسته‌جمعیِ دیتا نماند — حالا که دفتر مشترک است، یک لمسِ اشتباهی دیتای هر دو نفر را می‌برد.
  `tests/test_no_reset_button.py` نگهبانِ این تصمیم است تا دوباره برنگردد. اگر روزی برای
  دیباگ لازم شد، از تاریخچه‌ی گیت برداشته شود و **هرگز** به کیبورد اضافه نشود.
- **بدهی و طلب (سومین نوع کارت):** حالا سه شاخه داریم — تراکنش، بدهی/طلب، هدف — و روتر و
  استخراج‌گر نیتِ هر عدد را به شاخه‌ی خودش می‌فرستند. یک پیام/ویس می‌تواند چند نیتِ هم‌زمان
  داشته باشد و برای هرکدام کارتِ جدا ساخته می‌شود. تسویه (کامل/جزئی) روی همان رکوردِ باز
  اعمال می‌شود، نه رکوردِ تازه.
- **خانوار:** هر کاربر عضو یک خانوار است (تک‌نفره یا مشترک). همه‌ی تراکنش‌ها/اهداف/بدهی‌ها
  با `household_id` ذخیره می‌شوند و `user_id` فقط «ثبت‌کننده» است. اهداف تجمعی‌اند و
  آلارم‌ها به همه‌ی اعضا می‌رود. دعوت با لینکِ یک‌بارمصرفِ deep-link تلگرام انجام می‌شود و
  خودِ لینک نقشِ مجوزِ دسترسی را هم دارد (بدون دست‌زدن به `ALLOWED_USER_IDS`).
- **روترِ نیت (intent-first):** حالا هر پیامِ متنی اول از یک کالِ کوچکِ روتر می‌گذرد که
  نیت را می‌خواند (ثبت/ویرایش/هدف، سؤالِ دیتایی، یا گفتگو: قابلیت‌ها/مشاوره/چت/بی‌ربط) و
  فقط کانتکستِ مرتبط را جلو می‌برد. باگِ «باشه»ِ همیشگی حل شد: پیام‌های گفتگویی همان‌جا
  پاسخِ طبیعی می‌گیرند، سؤالِ دیتایی به ابزارهای خواندنی می‌رود، و ثبت به خط لوله‌ی
  استخراج/چانکینگ. ویسِ کوتاه اگر چیزی ثبت نکرد، به روتر می‌افتد تا سؤال/گفتگو جواب بگیرد.
- محاسبات از LLM گرفته شد و به سیستم سپرده شد (LLM فقط می‌فهمد چه باید حساب شود؛
  جمع/تبدیل را کد انجام می‌دهد). تبدیل ارز فقط با نرخی که **کاربر می‌دهد** انجام می‌شود.
- گزارش/جمع‌بندی، ارزها را **جدا** نگه می‌دارد (تبدیل خودکار نداریم).
- مدلِ «ساعت کاری»: بعد از پایان سهمیه‌ی روزانه، پیام‌ها در صف ذخیره می‌شوند و
  ساعت ۱۰ صبح (پیش‌فرض، قابل‌تنظیم) یک‌جا در یک درخواست LLM ثبت می‌شوند؛ اگر کاربر
  قبل از ۱۰ صبحِ روز بعد پیام داد، اول صف پردازش می‌شود.

## کارهای آینده (To-Do)
- [ ] **TLS برای درِ دومِ ورودی** (تصمیمِ فعلی: **عمداً معوق**، چون دامنه نداریم).
      لینکِ `/s/<token>` روی HTTP قابلِ شنود است. رخنه‌ی واقعی‌اش: هرکس لینک را ببیند
      می‌تواند خرجِ جعلی ثبت کند — و چون تراکنشِ کامل مستقیم `confirmed` می‌شود، در
      گزارش و مصرفِ هدف هم می‌نشیند. خواندنِ دیتا یا جابه‌جاییِ پول ممکن نیست و هر ثبت
      کارتش در تلگرام دیده می‌شود. جبرانِ فوری: `/shortcut` برای rotate کردنِ توکن.
      TLS بدونِ **نامِ دامنه** ممکن نیست (برای IP خام گواهی صادر نمی‌شود و Shortcuts
      گواهیِ self-signed را رد می‌کند). دو مسیرِ آماده هر وقت خواستیم:
      - **رایگان:** ساب‌دامینِ DuckDNS + Caddy (گواهیِ Let's Encrypt، تمدیدِ خودکار).
        دسترسی از ایران را بهتر نمی‌کند، فقط رمزگذاری می‌دهد.
      - **بهتر:** دامنه‌ی ارزان + Cloudflare Tunnel — هم TLS، هم دسترس‌پذیریِ بهترِ
        ایران، هم بدونِ پورتِ باز روی سرور.
      در هر دو حالت، پورتِ ۸۷۹۱ فعلی را نگه دار و TLS را کنارش بالا بیاور؛ بعد از سبز
      شدن `INGEST_PUBLIC_URL` را عوض کن. وگرنه یک مسیرِ کارکرده را کور می‌شکنیم.
      (دسترسیِ مستقیم به IPِ سرور از ایران در عمل تست شد و **کار می‌کند**، پس مزیتِ
      دسترس‌پذیریِ Cloudflare فعلاً بیمه است نه نیاز.)
- [ ] **گم‌نشدنِ ورودی وقتی شبکه قطع است:** شرتکات try/catch ندارد؛ اگر POST نرود ورودی
      از بین می‌رود. فعلاً راهنما می‌گوید متن را اول در یک فایلِ محلی هم بنویس. راهِ
      بهتر: یک شرتکاتِ «فلاشِ صف» که فایلِ محلی را بعداً یکجا بفرستد.
- [ ] **فرمتِ صوتیِ میان‌بر (حالا مسیرِ اصلی):** اپل فارسی را در Dictation پشتیبانی
      نمی‌کند، پس `Dictate Text` برای کاربرِ فارسی‌زبان اصلاً گزینه نیست و راهنما به
      `Record Audio` تغییر کرد. یعنی `m4a` از مسیرِ فرعی به **مسیرِ اصلی** تبدیل شد،
      در حالی که پذیرشش از سمتِ OpenRouter هنوز در عمل تست نشده. اگر جواب نداد: یا در
      شرتکات به فرمتِ دیگری encode شود، یا سمتِ سرور تبدیل کنیم (نیازمندِ ffmpeg — به
      کارِ GHCR گره خورده). علامتِ خرابی: خطای «سرویس هوش مصنوعی جواب نداد» فقط روی
      ویسِ میان‌بر، در حالی که متن سالم ثبت می‌شود.
- [ ] **API نرخ ارز:** اتصال به یک سرویس نرخ روز (دلار/طلا/کریپتو) تا تبدیل‌ها به‌روز و
      بدون پرسیدن از کاربر ممکن شود. تا آن زمان، نرخ را از کاربر می‌پرسیم.
- [ ] **سقف برای صفِ بعد-از-ساعت‌کاری:** الان کاربر می‌تواند با ادامه‌دادن، سقف روزانه را
      عملاً دور بزند (همه به فردا موکول می‌شود). برای بیزنس‌مدل باید صف هم محدودیت داشته باشد.
- [ ] **بیزنس‌مدل / سقف متغیر:** هرچه کاربر بیشتر پرداخت کند، سهمیه‌ی روزانه‌اش بیشتر/دیرتر.
- [ ] **GHCR برای دیپلوی:** ساخت image در CI و pull لایه‌ایِ سرور تا دیپلوی‌ها چندثانیه‌ای
      شوند (الان انتقال کل image روی لینک کند سرور ~۷ دقیقه است).
- [ ] **برگرداندن فال‌بک صوتی gpt-4o-mini-audio:** بعد از GHCR، ffmpeg را برمی‌گردانیم
      (لایه‌اش یک‌بار کش می‌شود) و تبدیل ogg→mp3 را برای این فال‌بک اضافه می‌کنیم.
- [ ] **بانک تگ سلسله‌مراتبی کامل‌تر + بازبینی tag_suggestions.**
- [ ] **بودجه‌بندی، ثبت درآمد، هشدار سقف بودجه، پیش‌بینی پایان ماه، داشبورد تحلیلی.**
- [ ] **هدف روی چند دسته (مثل «رستوران و کافه» با هم):** الان هر هدف فقط یک تگ را ردیابی
      می‌کند؛ باید هدف بتواند چند دسته را با هم جمع بزند. (روتر فعلاً حداقل سؤالش را
      صادقانه جواب می‌دهد که فقط رستوران حساب شده.)
- [ ] **اهداف چندماهه/سفارشی:** فعلاً هر هدف فقط برای ماهِ شمسیِ جاری است.
- [ ] **ارز خارجی در ردیابیِ هدف:** الان فقط تومان/ریال در مصرفِ هدف حساب می‌شود.
- [ ] **آلارمِ هدف با حذف تراکنش پایین نمی‌آید** (last_alert_level کاهش نمی‌یابد) — بعداً.
- [ ] **متن‌های خیلی طولانی‌تر از ۳ پارت** فعلاً رد می‌شوند؛ در آینده صف‌بندی/چانک بیشتر.
- [ ] **بدهی/طلب در گزارشِ ماهانه و ردیابیِ هدف:** الان بدهی‌ها فقط مانده‌ی باز را در انتهای
      گزارش نشان می‌دهند و در مصرفِ هدف حساب نمی‌شوند (چون هنوز خرج نشده‌اند). اگر بعداً
      «پرداختِ قسط» را هم خرج حساب کردیم، باید تصمیم بگیریم.
- [ ] **سررسیدِ ساختاریافته برای بدهی:** فعلاً `due_text` متنِ خودِ کاربر است؛ تاریخِ واقعی
      نیست، پس یادآوریِ خودکارِ سررسید نداریم.
- [ ] **مدیریت خانوار از داخل ربات:** حذفِ عضو، تغییرِ دسترسیِ یک عضو، و باطل‌کردنِ لینکِ
      دعوتِ استفاده‌نشده. (فاز اول: دو نفر، پس فعلاً لازم نشد.)
- [ ] **حریم خصوصی در خانوار:** فعلاً همه‌چیزِ دفتر برای هر دو عضو دیدنی است (خواستِ فعلی
      همین است). اگر بعداً «خرجِ شخصی/مخفی» خواستیم، باید فلگِ private اضافه شود.

## بازخوردهای استفاده‌ی واقعی (اعمال‌شده)
- **ممیز در مبلغ:** `parse_amount` همه‌ی غیررقم‌ها را دور می‌ریخت، پس «۱.۴ دلار» بی‌صدا
  ۱۴ دلار ثبت می‌شد. حالا «,» و «٬» هزارگان‌اند و «.»/«٫»/«/» فقط وقتی اعشارند که
  یک‌بار آمده باشند و بعدشان ۱ تا ۲ رقم باشد («۱.۵۰۰» یعنی ۱۵۰۰، «۱.۴» یعنی ۱.۴).
- **ارز خارجی در گزارش گم می‌شد:** `to_rial` برای هر واحدی جز تومان/ریال صفر می‌دهد، پس
  خرجِ دلاری نه در مجموع بود نه در تفکیک دسته. حالا بخشِ جدا دارد و تعدادِ تراکنش‌های
  ارزیِ کنارگذاشته‌شده هم صریح گفته می‌شود. (تبدیل خودکار همچنان نداریم.)
- **منو در دسترس نبود:** دکمه‌ی Menu تلگرام تا وقتی `setMyCommands` صدا نشود اصلاً ظاهر
  نمی‌شود. حالا دستورها ثبت می‌شوند، `/menu` اضافه شد، و کیبوردِ پایین `is_persistent` است.
- **ویرایش بازخورد نداشت:** هر پرسشِ ویرایش دکمه‌ی «انصراف» دارد، بعد از ویرایشِ موفق
  یک تأییدِ صریح می‌آید («✅ مبلغ شد …»)، و پیام‌های موقت پاک می‌شوند.

## کارهای باز (بازخوردِ استفاده)
- [ ] **ویرایشِ تراکنشِ عضو دیگر با حرف‌زدن کار نمی‌کند:** `transactions.apply_update` هنوز
      `user_id` را چک می‌کند در حالی که دفتر مشترک است (اهداف و بدهی‌ها به خانوار منتقل
      شدند و این یکی جا ماند). دکمه‌ها و حذف کار می‌کنند ولی مسیرِ گفتگویی بی‌صدا شکست
      می‌خورد — بدترین حالت، چون شبیهِ «نفهمیدن» دیده می‌شود.
- **✅ ثبتِ دوباره‌ی خرج‌های همان روز (حل شد):** فرضیه درست بود و کاربر با یک مثالِ روشن
      تأییدش کرد — ثبتِ ششم از شرتکات، شش کارت می‌ساخت. دو علت روی هم بود: (۱) مسیرهای
      یک‌شات (شرتکات و صفِ صبح) تاریخچه‌ی روز را به مدلِ استخراج می‌دادند، (۲) مسیرِ
      شرتکات طرفِ دستیار را در حافظه ذخیره نمی‌کرد، پس آن تاریخچه رشته‌ای از پیام‌های
      بی‌جوابِ کاربر بود و دقیقاً شبیهِ «فهرستی از خرج‌های ثبت‌نشده» خوانده می‌شد.
      درمان ریشه‌ای: به این دو مسیر اصلاً تاریخچه داده نمی‌شود (یک‌شات‌اند و کانتکست لازم
      ندارند)، طرفِ دستیار هم ثبت می‌شود، و پرامپت خطِ قرمزِ «فقط از آخرین پیام استخراج کن»
      را دارد. `tests/test_no_duplicate_reextraction.py` نگهبانش است.
- [ ] **عنوانِ «نامشخص» در ثبت‌های میان‌بر:** باید با دیدنِ `transcript` تفکیک شود که
      مشکل از پرامپت است، از فرمتِ صوت، یا از کوتاهیِ خودِ گفته.
